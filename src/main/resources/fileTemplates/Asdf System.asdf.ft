;;; -*- mode: Lisp; Base: 10 ; Syntax: ANSI-Common-Lisp ; Package: CL-USER ; buffer-read-only: t; -*-
;;; Author: ${USER}
;;; Date: ${YEAR}-${MONTH}-${DAY}

(asdf:defsystem :${NAME}
    :description "${NAME}"
    :version "1.0"
    :author "${USER}"
    :depends-on ()
    :components ())